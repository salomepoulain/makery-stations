# ============================================================================
#  LINE COOK STATION: MESS (Local Artifacts)
# ============================================================================
# List files or folders that pile up while this cook is working.
# When the Head Chef calls `bake germs`, everything listed here is deleted.

report/**/*.aux
report/**/*.bbl
report/**/*.blg
report/**/*.log
report/**/*.out
report/**/*.toc
report/**/*.lof
report/**/*.lot
report/**/*.fls
report/**/*.fdb_latexmk
report/**/*.synctex.gz
report/**/missfont.log
